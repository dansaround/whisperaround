{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the WhisperAround windows. Clipboard, global-shortcut and window layout are driven from Rust, so only core defaults plus dragging/hide are exposed to the webview.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-hide"
  ]
}
