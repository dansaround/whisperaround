{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the WhisperAround main window. Clipboard and global-shortcut are driven from Rust, so only core defaults are exposed to the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
